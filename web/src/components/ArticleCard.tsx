/**
 * Kept so the section, tag, author and index pages keep working unchanged.
 * The real implementation is Card, which takes a `shape` - see lib/media.ts.
 */
export { Card as ArticleCard } from './Card'
